#!/usr/bin/env ruby

basedir = File.dirname(__FILE__) + '/..'
require 'rubygems'
require "#{basedir}/lib/uberblog"
require 'uberblog/exec'
require 'yaml'

conf = File.open("#{basedir}/config/blog.yml") { |file| YAML.load(file) }
cmd = Uberblog::Publisher.new(conf)
cmd.execute