#!/usr/bin/env ruby

require 'pathname'
$baseDir = Pathname.new(File.dirname(__FILE__) + '/..').realpath.to_s

require "#{$baseDir}/lib/uberblog"
require 'uberblog/cli'

cmd = Uberblog::Publish::Install.new $baseDir
cmd.exec ARGV