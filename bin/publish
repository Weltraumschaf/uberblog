#!/usr/bin/env ruby

require 'pathname'
$baseDir = Pathname.new(File.dirname(__FILE__) + '/..').realpath.to_s

require "#{$baseDir}/lib/uberblog"
require 'uberblog/cli/publish'

cmd = Uberblog::Cli::Publish.new $baseDir
cmd.logger = Uberblog::CliLogger.new
cmd.exec ARGV