#!/usr/bin/env ruby

require 'pathname'
$baseDir = Pathname.new(File.dirname(__FILE__) + '/..').realpath.to_s

require "#{$baseDir}/lib/uberblog"
require 'uberblog/cli'

cmd = Uberblog::Cli::Install.new $baseDir
cmd.logger = Uberblog::Logger.new
cmd.exec ARGV
