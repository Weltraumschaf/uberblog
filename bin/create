#!/usr/bin/env ruby

require 'pathname'
$baseDir = Pathname.new(File.dirname(__FILE__) + '/..').realpath.to_s

require "#{$baseDir}/lib/uberblog"
require 'uberblog/cli/create'

cmd = Uberblog::Cli::Create.new $baseDir
cmd.logger = Uberblog::Logger.new($stdout, $stderr)
cmd.exec ARGV